# Margo v0.0.1 requirement-to-task traceability matrix

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prove that every section and acceptance criterion in the accepted Goshtoso Markdown design has an owned implementation task, executable evidence, and fresh-review promotion gate.

**Architecture:** Section requirements map to the smallest producing tasks. Acceptance criteria map to focused evidence and the integration gate that combines it. The roadmap owns cross-lane sequencing; subsystem plans own code and tests; this matrix owns coverage accounting only.

**Tech Stack:** Markdown traceability tables, Go test evidence, browser/platform evidence, SHA-256 identities, Git commit/tree review records.

## Global constraints

- Normative source is `/private/tmp/gs-goshtoso-markdown-design/docs/GOSHTOSO_MARKDOWN_DESIGN.md` at accepted HEAD `bfcf296db63eb18b5e54d61ceb3156c193b98ecd` and review identity `426068fae1cf60c95bfd5e7b00b9721958355cf115f58cab32770394a1328366`.
- The current normative file SHA-256 is `6b41bc995de83d6835a96fd9e73ddb59d642e87bd6ce13aaac3c0c7852499fc8`. A changed byte invalidates this matrix until a fresh review remaps it.
- Requirement IDs `S01-S34` cover every level-two and level-three design heading from Summary through References. Acceptance IDs `AC01-AC22` cover every bullet under Acceptance criteria, in source order.
- `SG01` is the later mandatory public-page social-metadata requirement. `AUTH01` is the later authority correction: no production origin is approved by the design.
- R7-F1 through R7-F4 are binding review corrections: literal deletion-aware staging manifests; D4/D5 checked browser environment files; assigned PowerShell/browser-receipt paths with one Chromium schema; and the explicit `zip.empty_directory` rejection rule. R8-F1 through R8-F3 are also binding: I1b passes an immutable source `SelectionOracle` into ZIP normalization and compares only allowed streams while independently hashing source exclusions and ZIP rejection rows; M0/D4/D5 use absolute repository-root Windows browser paths with checked-JSON path equality; and I1a originally owns the exact-artifact proxy handoff consumed by I1b/I2 with `GOSUMDB=off`, local `go.sum` verification, network-denial evidence, and cleanup receipts across root/charts/pdf/cmd gates. R9-F1 and R9-F2 supersede that ownership wording: I1a is the sole authority that freezes a complete direct/transitive superset proxy and immutable handoff; I1b, H1/H6, I2, I3, and I4 consume byte-identical path/content/artifact/env/network/cleanup identities; I2 never extends or mutates the proxy; and the canonical provenance record carries every handoff path and digest plus mismatched manifest, environment, network, and handoff-record rejection fixtures. R10-F1 and R10-F2 bind one `margo-canon-json-v1` typed-record encoding, `margo-realpath-v1` path normalization, exact `proxy-handoff-v1` and artifact-set preimages, and exact `ManifestRow`/`ExclusionRow`/`ZipRejectionRow` streams with domain-separated SHA-256 prefixes, unsigned numeric fields, UTF-8 path rules, deterministic sort keys, and encoding/order/field/path/preimage mutation fixtures; all consumers validate these bytes before module commands. The R10 independent reviewer identity is `099505ec1c33def9a9021a0460d26d65e6d8fd91bc458b6c661a47a3dfc29f71` for target `/private/tmp/margo-v001-plan-integration-r10` head `5ef286c0b2c1486bb46fbd9360dddaf884d99f5b` tree `6a986c8bcd07268153c56b1ed44953d276edefe5`, and this correction preserves the accepted design SHA-256. R11-F1 is binding: `canonBytes` uses one `encoding/json.Encoder` with `SetEscapeHTML(false)`, removes exactly its one terminal LF for single-record bytes, and `canonLine` adds exactly one LF only for line streams; newline, trim, and trailing-whitespace mutation fixtures fail closed. R11-F2 is binding: the immutable proxy handoff records separate POSIX and Windows environment paths and domain-separated SHA-256 values plus runner selection; consumers verify both raw files before deriving the selected alias. The R11 reviewer identity is `453c06d78446944d4f5f1ffaa40cc9fd23ac10e3eabab576611e5ecde0c0d075` for target `/private/tmp/margo-v001-plan-integration-r11` head `445458d693f69048e85f341fb282d2ef071269be` tree `758f2384528fc03c8bcb2c21c8ff79b33badbb61`; accepted design SHA-256 remains `6b41bc995de83d6835a96fd9e73ddb59d642e87bd6ce13aaac3c0c7852499fc8`.
- R12-F1/F2 are binding: I1a owns the exact `proxy-manifest-v1` schema/hash/empty-stream rule and `margo-realpath-v1` algorithm; I1b, H1/H6, I2, I3, and I4 realpath and byte-compare every handoff path and manifest before commands. I2/I3/I4 use the roadmap's executable ambient-cleared `readonly_module_command`/`readonly_module_gate` scope for every independent Go/generator command, with authoritative `GOPROXY`, `GOSUMDB=off`, `GOWORK=off`, `GOFLAGS=-mod=readonly`, and per-command module-file hashes. The R12 reviewer identity is `4565cbd1d1e6608ea9f073b6a68b6d0120d5223dbf087270ff4bb7327b2f4023` for target `/private/tmp/margo-v001-plan-integration-r12` head `5b5713c0798305b765f841dea24bb6fbad0a0f69` tree `ad53503766d3bea55aadbbf7cf2219a4e74b202f`; accepted design SHA-256 remains `6b41bc995de83d6835a96fd9e73ddb59d642e87bd6ce13aaac3c0c7852499fc8`.
- R13-F1/F2/F3 are binding: both `readonly_module_*` and `readonly_lane_*` scopes unset `HTTP_PROXY/http_proxy`, `HTTPS_PROXY/https_proxy`, `ALL_PROXY/all_proxy`, and `NO_PROXY/no_proxy` before authoritative variables; the contamination fixture records the sorted post-unset environment and fails closed on any lowercase survivor. I4 builds `cmd/margo` only through the readonly helper with explicit output paths, then runs the wrapper after disposable-proxy cleanup; subsystem command lines use the exact lane scope rather than ambient prefixes. The R13 reviewer identity is `d3e489f2fa97f42661ebd5c427f8e7bfa08f3db14d86959592f734aa445382f1` for target `/private/tmp/margo-v001-plan-integration-r13` head `c1918c0df0f0a5a42f5b59f6ad1e8f9e206555f6` tree `49bbf0079b83ebd84866385051b8b686da09c4ad`; accepted design SHA-256 remains `6b41bc995de83d6835a96fd9e73ddb59d642e87bd6ce13aaac3c0c7852499fc8`.
    - R14-F1 through R14-F4 are binding: C5-C7 final root source acceptance precedes I1a's sole complete-superset proxy freeze; I1b consumes that immutable handoff and source, and the DAG/schedule contain no pre-freeze root source path. Both readonly gates propagate every helper failure with `|| return 1` and verify requested cleared-environment records byte-for-byte after a successful write. I4 builds against `file://$MARGO_MODULE_PROXY` before cleanup, requires all four module `go.mod`/`go.sum` pairs and gates each once before and after cleanup, and records a nonzero outside-superset module/artifact probe. The R14 reviewer identity is `aec776fb1deafd28888b9ec47c26ca74dadc9429d855c92c4a0f002f638dcc78` for target `/private/tmp/margo-v001-plan-integration-r14` head `f3cda6ab4eac59bd466b46461f0735b9a19b2939` tree `5c34d99d8b63438f28552f1ec7f6af7ad69037f9`; accepted design SHA-256 remains `6b41bc995de83d6835a96fd9e73ddb59d642e87bd6ce13aaac3c0c7852499fc8`.
    - R15-F1 through R15-F3 are binding: C6 consumes T6's accepted API evidence and freezes `margo/head-owner-selection/v1` before C7; C7 consumes it unchanged; I1a verifies compatibility and cannot switch owner or edit C7 after source/proxy freeze. The DAG/schedule put this selection before C7 and C5-C7 final source before I1a. I4's self-contained `set -eu` outside-superset sequence canonicalizes absolute `fixturePath`/`artifactPath`, checks every write/status, emits and validates the fixed no-LF evidence record with `artifactAbsent`, and gates all four module file pairs before/after cleanup. The R15 root transfer wording is superseded by R16: C0 owns and freezes the root `go.mod`/`go.sum`/tool-lock plus exact Goshtoso pin before C5; C5-C7 and I1a are readonly consumers with hash gates. The R15 reviewer identity is `b4b11d1417e92ca1f3999f3ffef86be7e9372729b17271aa3aec5f03fa55a9b1` for target `/private/tmp/margo-v001-plan-integration-r15` head `f9c88a5ba1b86bbc7e475d97e4ea86e50f538d12` tree `834beb8fee290517808cdc9f7d9fbd9b9749cd33`; accepted design SHA-256 remains `6b41bc995de83d6835a96fd9e73ddb59d642e87bd6ce13aaac3c0c7852499fc8`.
- Every task's reviewer records its mapped IDs. Integration reviewers reject missing IDs, evidence produced by an unaccepted commit, or evidence run with a hidden `go.work`.
- R16-F1 is binding: C0 owns the exact reviewed `github.com/araihu/goshtoso v0.1.2` pin and sole root `go.mod`/`go.sum`/tool-lock write before C5. `rootModuleToC5.v1` records C0 pre/post hashes, commit/tree, destination, and complete staged status/summary/raw records; C5-C7 consume the files read-only and prove hashes unchanged around templ generation/tests. I1a consumes the C0 record and final C5-C7 source, verifies source-bound module identity, and creates the complete proxy/handoff without writing or staging root module files. The R16 target is `/private/tmp/margo-v001-plan-integration-r16` head `26a4784f75ffbcf29c23b55aed068c5413a07522` tree `f5d54fc1ce189a410bebf5f8fa410a978fa1edb6`; accepted design SHA-256 remains `6b41bc995de83d6835a96fd9e73ddb59d642e87bd6ce13aaac3c0c7852499fc8`.
- No row authorizes the post-v0.0.1 batch implementation, a site generator, deployment, repository mutation, or a production domain.
- Task inventory is 49 implementation tasks with 49 independent RED/GREEN/REFACTOR/commit checkpoints: `C0-C8` (9), `T0-T6` (7), `M0-M7` (8), `H1-H6` (6), `P1-P7` (7), `D1-D5` (5), and `O1-O7` (7). Integration tasks `I0-I4` add no implementation-task count.
- F01-F08 are binding correction gates: C0/T0/H1/P1/O5 provisioning; M0 browser harness; T1/T6/I1a Table validation and authorized handoff; I1a-I4 local-proxy/no-replace graph; H1-H5 full-value alternatives; O5/O6 exact engine selection; C7/D5/O5/I4 authority record; O7/I4 evidence schema and transport. Replacement review gates R2-A through R2-F refine those owners: T1 owns `components/table/component.go` and every public fragment; H1-H5 own adjacent complete-value alternatives; C0/M0/P1 own locked tool and runner bootstrap; C7/O5/I4 consume a versioned authority record; O7 owns evidence inputs/checker and all-ID coverage; T6/I1a own the external Goshtoso handoff without reviewer publication. Round-3 gates add exact M0 versus M3-M7 path ownership, staged-path equality, conditional/escaped/bounded ordered chart alternatives, non-self-referential module provenance, and authenticated Node release signatures. Round-5 gates add Go module-zip selection normalization, all-lock-entry npm cache receipts, deletion/status/mode-aware staging, compile-time policy storage with root-to-family proxy dispatch, compiler mismatch, and zero-byte policy failure. Round-6 gates add the C0-pinned x/mod normalization dependency and source digest, one canonical source/ZIP wrapper with identical rules and explicit ZIP metadata rejection, and one executable M0 provision-node/populate/check/environment-file sequence with immutable cache-root identity. Round-7 gates require literal quoted Git pathspecs with deletion-aware `git add --all --`, independent name-status/summary/raw manifests, deletion/rename/type negative fixtures, D4/D5 checked environment files on POSIX and Windows, clean PowerShell variable assignment with one browser receipt schema, and one explicit ZIP empty-directory rejection rule. Round-9 gates make I1a the sole proxy authority, require one complete superset manifest before any consumer, freeze the absolute proxy/environment/network/cleanup/handoff tuple, forbid I2 extension or mutation, and require all consumers plus I1b to verify the canonical path/content/artifact/env/network/cleanup digests and reject each mismatched-record fixture. Round-11 gates add the exact Encoder-LF removal/canonLine rule with newline, trim, and trailing-whitespace mutation fixtures, and dual-platform proxy environment paths/digests with explicit runner selection, byte verification, and selected aliases; the R11 reviewer identity and target/head/tree are recorded in the preceding correction note.

## Routed ownership and serialized transfers

| Path | Exclusive owner | Forbidden concurrent owners | Serialized successor / consumer |
|---|---|---|---|
| `go.mod`, `go.sum`, `tools/toolchain.lock`, `integration/root-module-transfer.v1.json` | Core C0 exclusively; C0 pins Goshtoso before C5 and records `rootModuleToC5.v1` | C1-C8, optional modules, I1a, and all other integration lanes; no successor may write or stage root module files or the transfer record | C0 stages exactly the three module/tool paths plus the transfer record with complete status/summary/raw evidence and pre/post hashes; C5-C7 consume the immutable post-write bytes read-only and prove hashes unchanged around every command; I1a verifies the C0 record and stages only its two table compile fixtures plus external proxy artifacts. I1b reads the sorted `moduleSuperset[]` inventory and no later task rewrites root module files. |
| `table_adapter.go`, `code_adapter.go` | Core C5 | C1-C4, C6-C8, charts/pdf/deck/CLI | C6 consumes accepted C5 adapters |
| `manifest.go` | Core C8 | C1-C7 and O1-O7 | No successor before v0.0.1 release |
| `tools/verify-table-handoff.go` | Goshtoso T6 | T1-T5 and Margo root lanes | I1a reads the accepted verifier and receipt only |
| `.claude/skills/using-goshtoso/components-reference.md` | Goshtoso T5 | T0-T4 and T6 | T6 records the generated digest; I1a reads it |
| `assets/runtime/deck-layout.js`, `assets/runtime/deck-layout.test.mjs` | Deck D4 | Mermaid M3/M6 and Deck D1-D3 | D5 and I3 consume accepted layout output |
| `assets/runtime/margo-runtime.js` | Mermaid M3 | M0, M4-M7 | M6/M7 consume after M3 |
| `assets/runtime/svg-normalize.js` | Mermaid M4 | M0, M3, M5-M7 | M5 consumes normalized output |
| `assets/runtime/svg-validate.js` | Mermaid M5 | M0, M3-M4, M6-M7 | M6 consumes validator |
| `assets/runtime/mermaid.js` | Mermaid M6 | M0, M3-M5, M7 | M7 and I2 consume queue |
| `assets/runtime/readiness.js`, `testdata/runtime/*` | Mermaid M7 | M0, M3-M6 | I2 consumes immutable readiness/composition hashes |
| `test/browser/node-toolchain.lock`, `bootstrap-node.sh`, `bootstrap-node.ps1`, `run-playwright.sh`, `run-playwright.ps1`, `populate-npm-cache.mjs`, `npm-cache-receipt.schema.json`, `package.json`, `package-lock.json`, `playwright.config.mjs`, `browser-lock.json`, `install-browser.sh`, `install-browser.ps1`, `test/browser/harness/*.mjs`, `test/browser/fixtures/*.html`, `test/browser/fixtures/*.svg`, `test/browser/fixtures/*.css` | Mermaid M0 harness, all-lock-entry npm cache bootstrap/receipt, and same-process absolute-tool runners | M3-M7 specs and runtime files | M3-M7 invoke the accepted runners; no ambient npm/npm exec, consumer cache population, or browser install is accepted |
| `test/browser/specs/runtime-schema.spec.mjs` | Mermaid M3 | M0, M4-M7 specs | M4/M6 consume schema parity |
| `test/browser/specs/svg-normalize.spec.mjs` | Mermaid M4 | M0, M3, M5-M7 specs | M5 consumes normalized vectors |
| `test/browser/specs/svg-validate.spec.mjs` | Mermaid M5 | M0, M3-M4, M6-M7 specs | M6 consumes validator vectors |
| `test/browser/specs/mermaid-queue.spec.mjs` | Mermaid M6 | M0, M3-M5, M7 specs | M7 consumes queue |
| `test/browser/specs/readiness.spec.mjs`, `test/browser/specs/composition.spec.mjs` | Mermaid M7 | M0, M3-M6 specs | I2 consumes browser reports |
| `fingerprint.go` | Core C1 until accepted C1, then Core C8 | No concurrent C2-C7 or integration writer | C8 is the named serialized successor |
| `integration/root-module-provenance/v0.0.1.json`, `integration/root-module-provenance/normalize.go`, `integration/root-module-provenance/normalize_test.go`, `integration/root-module-provenance/testdata/*` | Integration I1b | H1-H6 and I2 while I1b writes | H1 consumes `moduleSourceCommit`/`moduleSourceTree` and verified proxy bytes plus the unchanged I1a handoff; outer evidence carries separate `receiptCommit`/`receiptTree`, rejects self-reference, and I2 writes separate provenance without editing proxy artifacts. I1b owns the C0-pinned x/mod-backed `margo/go-module-zip-selection/v1` wrapper, the immutable source `SelectionOracle`, source/ZIP allowed-file manifests, independently hashed source exclusions and ZIP rejection evidence, raw ZIP mode/metadata preflight, and nested-module/VCS/vendor/symlink/non-regular/invalid-path fixtures. The provenance schema records `proxyHandoffSchemaVersion`, `proxyHandoffCanonicalEncoding`, `proxyHandoffRealpathAlgorithm`, `proxyArtifactSetEncoding`, `proxyManifestPath`, `proxyManifestSHA256`, `proxyArtifactSetSHA256`, `proxyEnvPOSIXPath`, `proxyEnvPOSIXSHA256`, `proxyEnvWindowsPath`, `proxyEnvWindowsSHA256`, `runnerEnvironmentSelection`, `networkPolicyCommand`, `networkPolicyCommandSHA256`, `networkPolicyReceiptPath`, `networkPolicyReceiptSHA256`, `handoffRecordPath`, `handoffRecordSHA256`, `cleanupReceiptPath`, and `cleanupReceiptSHA256`; `NormalizeModuleZip` cannot derive or silently copy source-only exclusion rows. Every row uses the exact typed canonical JSON fields, UTF-8 path validation, unsigned size/mode encoding, deterministic comparator, LF record terminator, and domain-separated allowed/source-exclusion/ZIP-rejection SHA-256 preimage from the roadmap. |

Every listed path has one active owner at a time. A successor may read an accepted predecessor commit, but cannot edit its files until the named serialized transfer gate. M0 runner paths are distinct from M3-M7 spec paths; staged ownership must include the runner scripts, cache bootstrap, and receipt schema exactly when M0 changes them. H6's root proxy test and fixtures are separate from H1's extension/accessibility files.

The ownership audit passes literal quoted Git pathspecs directly to `git add --all --`, never through a shell-expanded array. It compares complete cached `--name-status` records, `--summary` mode/type records, and `--raw` rename/status records against independent path-limited manifests generated with those same literal pathspecs; all use `--diff-filter=ACDMRT --find-renames --find-copies`, so absent tracked files remain deletion records. It rejects any staged path not in that manifest, any forbidden path, unowned deletion, rename source or destination, type or mode change, or fixture/generated mutation. Each task's temporary negative fixture deletes an owned tracked file, renames an owned file across its boundary, and replaces an owned regular file with a directory or symlink; the manifest mismatch must fail closed before restoration. M0 harness paths and M3-M7 browser spec paths are separate sets; no shared `test/browser` or `assets/runtime` tree path is accepted as an owner substitute.

## Normative section coverage

| ID | Normative section | Producing tasks | Required evidence and promotion gate |
|---|---|---|---|
| S01 | Summary, lines 8-26 | `T0-T6`, `C0-C8`, `M0-M7`, `H1-H6`, `P1-P7`, `D1-D5`, `O1-O7` | Exact tool/module provisioning, semantic Goshtoso output, authorized Table handoff, pinned browser/runtime, all format gates; `I4` confirms product boundary and no reverse Goshtoso dependency. |
| S02 | Goals, lines 27-50 | `C0-C8`, `T0-T6`, `M0-M7`, `H1-H6`, `P1-P7`, `D1-D5`, `O1-O7` | Each goal has focused RED/GREEN evidence in its owning plan; `I4` combines API, HTML, PDF, deck, CLI, identity, transaction, authority, evidence, and offline evidence. |
| S03 | Non-goals, lines 52-69 | `C1`, `C3`, `M1`, `H1`, `P6`, `D1-D2`, `O5-O7`, roadmap backlog boundary | Source/import audit proves no site generator, batch/glob execution, arbitrary JS/CSS/HTML, full Marpit engine, server Table, chart wildcard, Chromium download, or native pixel-identity promise. |
| S04 | Repository and module boundaries, lines 71-115 | Wave 0 interface, `C0`, `T0`, `H1`, `P1`, `O5`, `I0-I4` | Exact module/tool predecessor pins include direct `github.com/araihu/goshtoso v0.1.2` and `golang.org/x/mod v0.30.1-0.20251115032019-269c237cf350` sums, upstream commit, module-zip/source-manifest digests, and C0 lock ownership; C0's `rootModuleToC5.v1` transfers the exact root module bytes and pre/post hashes to C5 before semantic rendering; After C5-C7 final root source acceptance, I1a freezes one complete superset `proxy-manifest-v1` with fixed typed field order, module/version sort, lowercase 64-hex digests, strict unknown/duplicate-field policy, `margo-proxy-manifest-v1 NUL` hash and canonical empty `modules:[]` bytes, plus immutable path/content/artifact/POSIX-environment/Windows-environment/runner-selection/network/cleanup handoff under the exact `margo-canon-json-v1`/`canonBytes`/`canonLine`, `margo-realpath-v1` (`Abs -> Clean -> EvalSymlinks -> Clean -> separator/volume/UNC/case` with existing-path and exact-byte checks), `proxy-handoff-v1`, and artifact-set preimages. C5-C7 consume C0's root module files read-only; I1a consumes the unchanged C0 record plus final C5-C7 source and creates the proxy without root-module writes; I1b consumes the unchanged I1a handoff and final C5-C7 source; H1/I2/I3/I4 verify it without extension or mutation; `go list -deps` proves root has no optional imports; every discovered module passes the executable ambient-cleared readonly module or lane scope with authoritative GOPROXY, `GOSUMDB=off`, `GOWORK=off`, `GOFLAGS=-mod=readonly`, both-case proxy clearing, and pre/post `git hash-object go.mod/go.sum` assertions around each command; release order is root, charts, pdf, CLI. No GOROOT vendor or transitive templ copy can satisfy normalization. |
| S05 | Repository bootstrap gate, lines 116-131 | External Wave 0 plus `I0` reconciliation | Fresh settings/API evidence proves public `araihu/margo`, protected `main`, required checks, blocked destructive updates, license/security/ownership files, topics, and module-discovery CI before code promotion. |
| S06 | Compilation pipeline, lines 133-157 | `C1-C6`, `C8`, `P1`, `D3`, `O5` | Stage-order tests and failure fixtures prove parse, normalize, requirement collection, policy, extensions, resource validation, rendering, assets, optional export, then manifest; precommit failure produces no final artifact. |
| S07 | Core API, lines 158-263 | `C0-C5`, `C8` | C0-pinned Goshtoso module transfer/hash tests and API compile tests, snapshot/mutation tests, compiler-binding tests, immutable document/result tests, race tests, canonical vectors, typed Goshtoso adapter errors, compile-time storage of immutable `Policy.OutputBytes`/`EffectivePolicy`, no render-time recomputation, `ExtensionNode`/`ExtensionSession` delivery, and manifest identity pass at `C5-C7`, `I1b`, and `I2`; C5-C7 and I1a cannot mutate root module files; `table_adapter.go` and `code_adapter.go` are C5-owned and `manifest.go` is C8-owned. |
| S08 | Extension registry, lines 264-276 | `C4`, `H1`, `M2` | Freeze/order/missing-integration/session-isolation tests pass; duplicate names and mutation after `New` fail deterministically. |
| S09 | Markdown profile, lines 277-292 | `C2` | Goldmark fixture corpus proves CommonMark, selected GFM, footnotes, heading-ID collision suffixes, versioned profile identity, and no document-controlled extension enablement. |
| S10 | Tables, lines 293-355 | `T0-T6`, `C5`, `I1a-I1b` | T0 pins templ; T1 owns `components/table/component.go` and validates before `Instance.Render`, `TableHeadContentInstance.Render`, `TableRowsInstance.Render`, `TableRowInstance.Render`, `TablePaginationNavInstance.Render`, and every config fragment with zero-output errors for invalid `SortMode` and `SortValue`; T2-T5 cover `auto|none|server|client`, natural stable sorting, ARIA, no HTMX, and print restoration; T6 supplies an authorized immutable handoff consumed by I1a, with tag/tree equality and generated/CI evidence. |
| S11 | Frontmatter and configuration, lines 356-394 | `C2-C3`, `D1-D2`, `O5` | Unknown `goshtoso` keys fail, generic host metadata survives, presentation precedence is deterministic, security intersects, denied escalation fails, and explicit deck activation wins only within policy. |
| S12 | Security and policy, lines 395-487 | `C3`, `M2`, `M4-M5`, `H1-H6`, `P2`, `D2`, `I4` | Exact `margo-html-v1`, URL/root/symlink, sanitizer, CSS/token/font, data URI, YAML limit, exhaustion, cancellation, and network-denial corpora fail closed before artifact commit. |
| S13 | Mermaid, lines 488-636 | `M0-M7`, `C8` | M0 pins official Node `v26.5.0` and npm `11.17.0` source/archive URLs, npm package integrity, signed manifest, and per-runner executable digests for exactly darwin-arm64, darwin-x64, linux-x64, and windows-x64, plus Playwright/Chromium DOM/XML/CSS harness and one revision/archive digest per runner. M0 provisions Node/npm without requiring a cache, chooses one absolute cache root, populates every lock-resolved npm tarball by recorded integrity into that root, writes a per-package URL/integrity/byte receipt, reruns `--check --cache-root`/`-Check -CacheRoot`, and passes the unchanged environment file to the same-process runner; cache-root overwrite, missing, extra, stale, or tampered entries fail before offline `npm ci --cache`. The lock authenticates the signed manifest with trusted Richard Lau fingerprint `C82FA3AE1CBEDC6BE46B9360C43CEC45C17AB93C`, pinned key source/digest, and offline `gpgv` status check; wrong-key/bad-signature fixtures fail. Same-process shell/PowerShell runners use only absolute verified binaries, offline `ci`, local `--no-install` Playwright, locked Chromium, command-path/executable-digest/cache assertions, and network denial. Muamba lock pins `11.16.1`; directives/config channels fail before execution; every declared family passes the profile corpus; IDs/references/CSS normalize and validate with no regex fallback; profile or runner changes block release. PowerShell uses repository-root cwd, resolves `test\\browser` once to absolute script/cache/receipt/env paths, asserts the checked JSON path equals the D4/D5 `-EnvironmentJson` input, and proves clean-process ambient conflicts fail closed; provision and check emit identical Chromium path/revision/version/digest fields. |
| S14 | Runtime execution and readiness, lines 637-719 | `M3`, `M6-M7`, `P2`, `C8` | `margo-runtime/v1` schema, legal transitions, dependency order, forged/missing report, timeout, stable layout, font/Mermaid errors, duplicate placement, namespace isolation, and terminal projection tests pass. |
| S15 | Goshtoso Charts, lines 720-763 | `H1-H6`, `C4`, `I2` | Closed bar/line/pie/doughnut/categorical-scatter schemas accept YAML/JSON, reject unknown/invalid/non-finite/excess data with pointers and source positions, and emit Goshtoso-rendered static SVG plus an adjacent adapter-owned deterministic full-value table for every family. Conditional rows require `Category|Value`, `Series|Category|Value`, or `Series|Category|Sample|Value`; `Sample` without `Series` is forbidden. Canonical fields reject invalid UTF-8 and reversibly percent-escape pipe, percent, newline, CR, and controls. Exact ordered extraction must equal `Revenue|Development|12`, `Revenue|Production|18`, `Cost|Development|7`, `Cost|Production|9` for both bar orientations and line, `Desktop|40`, `Mobile|60` for pie/doughnut, and `Latency|p50|12`, `Latency|p95|18`, `Throughput|p50|30`, `Throughput|p95|42` for scatter; an order permutation mutation fails. `WithAccessibleData` receives the root output-byte policy through the compile-time `ExtensionNode`/`ExtensionSession` bridge, and H6's frozen-I1a-handoff test proves Compile-to-family propagation, compiler mismatch rejection, handoff manifest/environment/network/record mismatch rejection, and zero caller bytes on overflow; no runtime marker is accepted. |
| S16 | Asset supply chain and overrides, lines 764-796 | `M0-M1`, `C6`, `P1`, `I2-I4` | M0 locks Node/npm executable, browser package/archive provenance; P1 locks native/CDP runner and host-runtime evidence; Muamba strict sync/verify/generate check, embedded hashes/licenses, size/type/root/symlink controls, approved override identity, no render-time CDN/Node/npm/browser download, and generated-drift checks pass. |
| S17 | CSS, themes, and whitelabel rendering, lines 797-833 | `C3`, `C6`, `P1-P2`, `D3-D5` | Light/dark/Goshtoso/Minimal, scoped tokens, brand assets/fonts, print, header/footer/logo/icon/watermark, narrow/wide browser fixtures, and CSS rejection corpus pass through one document/theme pipeline. |
| S18 | PDF export, lines 834-844 | `P1`, `P7`, `O6` | PDF remains in the optional module; P1 pins root/chromedp dependencies independently and records `margo/pdf-platform-toolchain/v1` runner/bootstrap identities; CLI defaults native and requires explicit Chromium, with no engine fallback/download. |
| S19 | Native engine, lines 845-858 | `P3-P5`, `P7`, `I3-I4` | Windows WebView2 and macOS WKWebView direct PDF evidence passes; Linux yields an explicit supported or best-effort-unavailable report; Wails is not imported. |
| S20 | Chromium engine, lines 859-865 | `P6-P7`, `O5-O6`, `I4` | Explicit configured/installed executable, CDP control, recorded path/version, runtime-bearing render provenance, invalid/unavailable engine failure, no bundle/download, and no native fallback tests pass. |
| S21 | Page model and readiness, lines 866-886 | `P1-P2`, `P7`, `D4-D5`, `I3` | Shared HTML/print/brand/page model, A4/Letter, orientation/margins/page number, exact runtime identity/readiness, blocked request, timeout, cleanup, and report fields pass. |
| S22 | Slide decks, lines 887-957 | `D1-D5`, `P2-P7`, `O6`, `I3` | Flag/frontmatter activation, exact directive profile, static semantic slides, one-slide-per-page, no interactive features, shared theme/policy, terminal overflow decision, and per-slide failure diagnostics pass. |
| S23 | CLI, lines 958-1000 | `O5-O7` | `render --engine native|chromium` (native default), `validate`, `inspect`, `pdf`, `deck`, `schema chart`, `doctor`, and `version` success/failure fixtures prove thin library use, exit codes, stdout/stderr separation, engine selection/provenance/no fallback, authority-record flags, and deterministic reports. |
| S24 | Output commit contract, lines 1001-1062 | `O1-O4`, `O7`, `I4` | Bounded spool, no-replace, force replace, parent sync, cancellation, read-back, stdout, and injected pre/at/post-linearization failures classify exact `CommitOutcome` values without rollback claims. |
| S25 | Determinism and provenance, lines 1063-1116 | `C1`, `M1`, `M3`, `C8`, `P1`, `O7` | Canonical vectors prove compiler/document/artifact domains, instance and engine binding, terminal runtime projection, `ExecutionID` exclusion, artifact digest, version/dependency/asset/profile/engine provenance, and stable repeat results. |
| S26 | First backlog: batch and glob rendering, lines 1117-1150 | `C8`, `O5`, roadmap backlog boundary | Types reserve `AdjacentMapper`, `PreserveMapper`, and `FlatMapper`; v0.0.1 CLI rejects multi-source/glob/batch invocation and does not implement discovery, caching, aggregation, or index manifests. |
| S27 | Verification strategy, lines 1151-1152 | All subsystem plans and `I1a-I4` | Evidence is layered into focused RED/GREEN/REFACTOR, module, race/browser/security/platform, frozen-I1a-proxy/no-replace, authority-record/input, releasecheck, integration, and distinct fresh-review gates; each implementation checkpoint uses quoted literal Git pathspecs, deletion-aware `git add --all --`, independent name-status/summary/raw manifests, and temporary deletion/rename/type fixtures; I1b/H1/H6/I2 reject mismatched manifest, artifact-set, POSIX-environment, Windows-environment, runner-selection, network, handoff, or cleanup identities before module commands, parse and re-encode the exact typed handoff/manifest schemas, and reject path aliases, unknown fields, stream order, one-LF removal, trailing-whitespace, canonLine newline, and hash-preimage mutations; O7 fails absent or mismatched downloaded HTML/image evidence and incomplete S/AC/SG/AUTH/HEAD coverage; I4 fails missing root/charts/pdf/cmd `go.mod` or `go.sum`, propagates each module-gate failure, and records the nonzero outside-superset module/artifact probe. |
| S28 | Core and browser behavior, lines 1153-1194 | `T0-T6`, `C0-C8`, `M0-M7`, `I1b-I2` | Every listed parser, golden, mutation, race, binding, Table validation/zero-output/handoff, pinned-harness, browser, Mermaid, runtime, composition, identity, and extension test is named and commanded in its owner plan. Goshtoso module, generator, verifier, and Just command payloads run through the exact roadmap lane scope with `GOWORK=off GOFLAGS=-mod=readonly`, both-case proxy clearing, authoritative proxy, and hash checks; T0's one `go get` provisioning write is immediately followed by standalone readonly verification. C0's x/mod download and I1b wrapper tests use the pinned sums under readonly mode and never GOROOT vendor/transitive templ. I1a's only networked module population is complete superset proxy provisioning before any consumer; I1b/H1/H6/I2 consume the unchanged handoff, reject every path/content/artifact/POSIX-environment/Windows-environment/runner-selection/network/cleanup mismatch before `go`, never extend or mutate it, and exercise canonical row field, UTF-8, unsigned size/mode, sort, exact canonBytes LF removal, canonLine LF, trailing-whitespace, domain-prefix, and empty-stream fixtures. M0's only networked cache population is the explicit populate step after Node provisioning and before final `--check --cache-root`; all consumer browser commands use the unchanged checked environment and `npm ci --offline --cache` with the verified cache. Windows browser gates start from repository root, use absolute `test\\browser` paths, and assert the checked JSON path is exactly the D4/D5 input. |
| S29 | Charts and decks, lines 1195-1206 | `H1-H6`, `D1-D5`, `P2`, `I2-I3` | Valid/invalid schema, accessibility, Marpit diagnostics, page count, horizontal/vertical overflow, and direct-unvalidated-HTML terminal failure evidence passes. |
| S30 | Security, lines 1207-1236 | `C3`, `M2`, `M4-M5`, `H1-H6`, `P2`, `D2`, `I4` | All named active-content, scheme, SVG/MathML, CSS, Mermaid config/SVG, traversal, network, token/font, data URI, YAML, exhaustion, cancellation, and cleanup families have negative fixtures and fail before insertion/commit. |
| S31 | PDF and platform matrix, lines 1237-1255 | `P2-P7`, `O1-O4`, `O7`, `I3-I4` | Required Windows/macOS/Linux/Chromium, paper/orientation/brand/content, timeout/cleanup, four-outcome filesystem, and zero-byte stdout failure evidence is archived per platform. |
| S32 | Delivery sequence, lines 1256-1288 | Roadmap `I0-I4` and all subsystem tasks | Fresh integration commits enforce bootstrap, Table, core, Mermaid, charts, PDF/runtime, deck, CLI, then release gates; batch stays a subsequent backlog. |
| S33 | Acceptance criteria, lines 1289-1335 | `AC01-AC22` below | `I4` release reviewer rejects any criterion without an accepted producer commit and reproduced evidence. |
| S34 | References, lines 1336-1350 | `M1`, `M4-M5`, `D1-D2`, research record | Implementers inspect the pinned primary APIs/profile sources; references explain contracts but do not widen the normative feature set. |

## Acceptance-criterion coverage

| ID | Acceptance criterion | Producing tasks | Acceptance evidence and final gate |
|---|---|---|---|
| AC01 | Public protected repository and bootstrap before publication | Wave 0, `I0`, `I4` | Live repository/default-branch/ruleset/check/file/topic evidence, reconciled to the bootstrap tree before Wave 1. |
| AC02 | Importable lightweight root with composable templ output | `C0-C1`, `C5-C6`, `I1b`, `I4` | C0-pinned Goshtoso/templ root modules are accepted before C5; clean consumer plus the root `readonly_lane_command . "$MARGO_CHECKED_GOPROXY" go list -deps ./...` proves templ rendering and no chart/browser dependency. |
| AC03 | Snapshotted inputs, immutable documents, declared concurrency | `C1`, `C4`, `I1b` | Mutation, defensive-return, concurrent compile/render/session, cancellation, and race tests pass. |
| AC04 | Compiler fingerprint binding | `C1`, `C4`, `I1b` | Divergent compiler rejects before hooks/assets; equivalent compilers produce identical bytes and identities. |
| AC05 | Offline standalone with embedded assets | `M1`, `C6`, `I1b`, `I4` | Network-denied render succeeds from verified embedded bytes with no CDN/npm/Node path. |
| AC06 | Mermaid default restrictive mode and readiness | `M2-M3`, `M6-M7`, `I2` | Default fixture executes through `margo-runtime/v1`; forbidden config, task, dependency, report, and timeout fixtures fail closed. |
| AC07 | Mermaid 11.16.1, Muamba digest, claimed-family corpus | `M0-M1`, `M4-M5`, `M7`, `I2` | Strict four-runner Node/npm acquisition lock with official Node archive and npm source/integrity, trusted-key fingerprint/keyring digest and offline `gpgv` verification, Playwright/Chromium archive revision and digest per runner, browser harness lock, all-lock-entry npm cache and per-package receipt, and the one provision-node, fixed-cache-root, populate, final-check, unchanged-environment-file sequence; wrong-key/bad-signature/cache-missing/cache-tamper/extra/root-mismatch fixtures, Muamba lock/digest/license/profile checks, and every manifest family corpus pass; observed-profile, browser, runner, or cache identity change blocks release.  M0 also requires explicit browser receipt/path/revision/version/digest equality in both bootstrap modes and clean PowerShell assignments with ambient-variable removal. |
| AC08 | Mermaid config rejection and complete SVG/CSS normalization | `M0`, `M2`, `M4-M5`, `M7` | Frontmatter/init rejection, Chromium DOM/XML/CSS structural parsing, root/descendant ID and reference rewriting, parsed profile CSS, unique-root scoping, no regex fallback, and canonical reparse tests pass. |
| AC09 | Validated accessible static charts | `H1-H6`, `I2` | Closed schemas and source-aware negative fixtures pass; Goshtoso SVG contains required semantics and an adjacent adapter-owned deterministic table contains every bar (horizontal/vertical), line, pie/doughnut, and scatter value in exact ordered `Series|Category|Sample|Value` forms (with only the defined `Category|Value` omission for pie/doughnut). Conditional empties, reversible escaping, invalid UTF-8, every category/series/value, and permutation mutations are asserted. H6 compiles root and Charts through I1a's frozen proxy handoff, dispatches all seven family fixtures through `ExtensionSession.Render`, proves the exact stored `OutputBytes` reaches `WithAccessibleData`, rejects compiler and handoff mismatches before session bytes, and writes zero caller bytes on a limit failure; output has no runtime marker. |
| AC10 | Goshtoso Table with compatible four-mode behavior | `T0-T6`, `C0`, `C5`, `I1a-I1b` | Exact Goshtoso module and templ provisioning in C0/T0; zero-value auto/server, none, server, client, invalid-option, every public fragment validation/zero-output, ARIA, sort-cycle, no-HTMX, print tests, and authorized handoff pass. |
| AC11 | Frontmatter cannot elevate permissions | `C2-C3`, `D1-D2`, `I1b` | Host/document mismatch yields structured denial; accepted frontmatter never expands host policy. |
| AC12 | Raw HTML only through exact profile and no arbitrary CSS/active content | `C3`, `I4` | Deny default and full `margo-html-v1` bypass corpus fail before output; accepted fixture stays within the exact allowlist. |
| AC13 | Native PDF on Windows/macOS, explicit Linux, Chromium opt-in | `P1`, `P3-P7`, `O5-O6`, `I3-I4` | Exact module pins, `margo/pdf-platform-toolchain/v1` lock, per-platform runner/host-runtime evidence, `--engine native|chromium` selection/default tests, runtime provenance, no silent fallback, no unavailable-engine fallback, and no browser download pass. |
| AC14 | Shared whitelabel PDF/deck document and theme pipeline | `C6`, `P1-P2`, `D3`, `I3` | Same theme/brand identity and fixture bytes feed HTML, PDF page model, and deck output. |
| AC15 | Explicit deck activation only | `D1`, `O6`, `I3` | Ordinary Markdown remains a document; flag or accepted metadata activates deck; denied host policy wins. |
| AC16 | Repeated placements have isolated instance/runtime/execution routing | `M3`, `M7`, `C8`, `I2` | Duplicate instance rejection, distinct DOM/SVG/task/report namespaces, failure isolation, and stable document identity pass. |
| AC17 | Marpit profile and layout-engine overflow decision | `D2-D5`, `P2-P7`, `I3` | Compatibility/unsupported fixtures, static HTML/PDF, page count, and deliberate horizontal/vertical overflow pass every accepted engine. |
| AC18 | Core-only document fingerprint and artifact terminal-result identity | `C8`, `M3`, `P1`, `I2-I3` | Cross-engine document stability, instance/engine/runtime-sensitive artifacts, stable standalone rerun, and `ExecutionID` exclusion vectors pass. |
| AC19 | Typed filesystem outcomes and fully spooled stdout | `O1-O4`, `O7`, `I4` | Fault matrix distinguishes `not_committed`, `committed`, `committed_durability_uncertain`, `state_unknown`; validation failure emits zero stdout artifact bytes. |
| AC20 | Deterministic CLI/library fingerprints, digests, provenance | `C0`, `C8`, `P1`, `O5-O7`, `I1b`, `I4` | Library and CLI reports match canonical vectors and exact artifact SHA-256 while recording C0's pre-C5 Goshtoso `v0.1.2` pin plus required pinned tool/module/dependency/asset/profile/engine identities, the direct x/mod version/sums/upstream commit, `.info`/`.mod`/`.zip` byte hashes, module-zip/source-manifest digests and wrapper source digest, separate root-module `.info`/`.mod`/`.zip` byte hashes, Go 1.26.5 normalized source/ZIP allowed-file and deterministic typed `ManifestRow`/`ExclusionRow`/`ZipRejectionRow` streams with UTF-8 path, unsigned size/mode/hash fields, fixed sort keys, LF records, and domain-separated SHA-256 preimages, exact `margo-canon-json-v1`/`proxy-manifest-v1`/`margo-realpath-v1` proxy handoff and artifact-set schemas including the manifest field/order/hash/empty-stream rules and filesystem normalization order, explicit ZIP mode/metadata rejection, nested-module/VCS/vendor/symlink/invalid-path rules, non-self-referential `moduleSourceCommit`/`moduleSourceTree` plus outer receipt identity, authority receipt digest, explicit authority/homepage/subpage/preview input paths and SHA-256 values, and evidence record identities. Explicit empty ZIP directory entries reject with `zip.empty_directory`; a no-directory ZIP is the deterministic accepted equivalent. The proxy handoff evidence binds both POSIX and Windows environment file paths and domain-separated content digests, verifies the explicit runner selection before deriving `MARGO_PROXY_ENV`, rejects either platform byte/digest or canonical newline/trim mutation, and records the ambient-cleared readonly command evidence with per-command module-file hashes; I4 proves all four module file pairs before and after cleanup and records the outside-superset negative artifact probe. |
| AC21 | No render-time npm, Node, CDN, or browser download | `M1`, `C6`, `P6-P7`, `O6`, `I4` | Offline source audit and execution pass; M0's only networked Node/npm and package-cache population is explicit, ordered provision-node then populate after a fixed cache root, followed by final offline check and unchanged environment-file handoff; consumer runners require that existing verified cache and use `npm ci --offline --cache`; Chromium only uses an explicitly selected installed executable.  D4/D5 and M1-M7 consume only the checked environment file; explicit browser provision precedes both bootstrap modes, whose Chromium fields have one schema, and no cache mutation is allowed during check. |
| AC22 | Every module, browser, security, and platform gate passes | All 49 tasks, `I4` | Fresh release reviewer reproduces complete command matrix with accepted commit/tree, explicit evidence paths/records, no-replace modules, verified authority when public, and zero unmapped IDs. |

## Mandatory public-page and authority coverage

| ID | Binding update | Producing tasks | Acceptance evidence and fail-closed rule |
|---|---|---|---|
| SG01 | Initial-rendered route title, description, canonical/`og:url`, Open Graph, explicit X Card, and one validated preview image for every public/indexable HTML surface | `C7`, `D5`, `O5`, `I1b`, `I3`, `I4` | Parameterized unit fixtures prove one metadata set and absolute HTTPS values. Live `I4` verifies the machine-readable `AuthorityRecord`, rejects unlisted origin/route, checks asset URL/MIME/dimensions/bytes/SHA-256 evidence, receives explicit downloaded homepage and representative-subpage initial HTML plus preview-file paths, and requires `image/png`, exactly 1280 by 640, below 1,000,000 bytes. |
| AUTH01 | The accepted design grants no production-domain authority | Roadmap pre-site authority gate, `C7`, `D5`, `O5`, `I4` | Private/local output emits no invented canonical/OG/X URL. Public/indexable acceptance cannot start until an authorized record supplies `CanonicalOrigin`, routes, owner/host/deployment evidence, transport receipt, and canonical digest. Caller origin strings and candidate hostnames have no authority. |
| HEAD01 | Prefer one Goshtoso/App Shell head owner and never duplicate tags | C6 freezes `HeadOwnerSelection` from T6 API evidence before C7; `C7`, `I1a`, `D5` | C6 records canonical `margo/head-owner-selection/v1` with owner, primitive, accepted commit/tree, API source path, and SHA-256. C7 consumes it unchanged. I1a verifies the released Goshtoso API matches; mismatch fails and returns to C6/C7, never changes owner or edits C7. Initial HTML contains each required tag exactly once. |

## Execution-time coverage audit

The matrix itself changes no product file. The release integration worker performs these checks from the accepted release-candidate worktree:

- [ ] **Step 1: Freeze authority and coverage inputs**

```bash
test "$(git rev-parse HEAD)" = "$ACCEPTED_RELEASE_CANDIDATE"
sha256sum docs/GOSHTOSO_MARKDOWN_DESIGN.md
rg -n '^## |^### ' docs/GOSHTOSO_MARKDOWN_DESIGN.md
```

Expected: the design digest equals the locked digest above and the 34 design headings map one-to-one to `S01-S34`. Any mismatch stops the audit and requires a new matrix review.

- [ ] **Step 2: Prove every criterion has evidence**

```bash
readonly_lane_command cmd/margo "$MARGO_AUTHORITATIVE_GOPROXY" go test ./internal/releasecheck -run 'TestSectionCoverage|TestAcceptanceCoverage|TestEvidenceIdentity|TestEvidenceRecord|TestPublicInputs' -count=1 -args -evidence-dir "$MARGO_EVIDENCE_DIR" -commit "$MARGO_COMMIT" -tree "$MARGO_TREE" -authority-record "$MARGO_AUTHORITY_RECORD" -homepage-html "$MARGO_HOME_HTML" -representative-html "$MARGO_SUBPAGE_HTML" -preview-file "$MARGO_PREVIEW_FILE"
```

Expected RED before release evidence is assembled: missing record for at least `AC22`. Expected GREEN only when all `S01-S34`, `AC01-AC22`, `SG01`, `AUTH01`, and `HEAD01` records point to accepted commits and successful commands.

- [ ] **Step 3: Run the cross-module and public-page gates**

```bash
find . -name go.mod -not -path './.git/*' -print0 | sort -z | while IFS= read -r -d '' module; do
  module_dir=${module%/go.mod}
  readonly_lane_command "$module_dir" "$MARGO_AUTHORITATIVE_GOPROXY" go mod verify
  readonly_lane_command "$module_dir" "$MARGO_AUTHORITATIVE_GOPROXY" go vet ./...
  readonly_lane_command "$module_dir" "$MARGO_AUTHORITATIVE_GOPROXY" go test ./... -count=1
done
: "${MARGO_AUTHORITY_RECORD:?verified authority record required for public acceptance}"
: "${MARGO_HOME_HTML:?homepage initial HTML input required}"
: "${MARGO_SUBPAGE_HTML:?representative initial HTML input required}"
: "${MARGO_PREVIEW_FILE:?preview image input required}"
readonly_lane_command cmd/margo "$MARGO_AUTHORITATIVE_GOPROXY" go test ./internal/socialcheck -run TestProductionPages -count=1 -args -authority-record "$MARGO_AUTHORITY_RECORD" -homepage-html "$MARGO_HOME_HTML" -representative-html "$MARGO_SUBPAGE_HTML" -preview-file "$MARGO_PREVIEW_FILE"
```

Expected: all modules pass independently and the public-page check parses fetched initial HTML plus the preview image. If no canonical authority exists, only the public/indexable subgate is blocked; private/local artifact gates continue and must omit publication URLs.

- [ ] **Step 4: Record the fresh-review checkpoint**

```bash
git rev-parse HEAD
git rev-parse HEAD^{tree}
git status --short
```

Expected: accepted commit/tree match the evidence record and status is clean. Commit checkpoint for implementation execution: `chore: record v0.0.1 requirement coverage`. Preserve the release-candidate worktree until the distinct release reviewer accepts every row; do not tag, publish, or deploy from this task.

## Coverage conclusion

Coverage gaps: none. One external execution authority is intentionally absent: no approved `CanonicalOrigin` exists in the accepted design. That absence is not filled by a constant or candidate hostname; it blocks only public/indexable page acceptance until the bounded pre-site authority gate supplies a record. Private library, standalone, PDF, deck, and CLI work remains executable under the omission rules above.
